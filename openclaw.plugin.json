{
  "id": "openclaw-interaction-bridge-v2",
  "name": "OpenClaw Interaction Bridge v2",
  "description": "Bridge OpenClaw agent state, approval requests, and notifications to Snarling (physical display + A/B buttons)",
  "skills": ["skills"],
  "channelEnvVars": {
    "approval": ["OPENCLAW_APPROVAL_SECRET"]
  },
  "envVars": ["OPENCLAW_APPROVAL_SECRET"],
  "contracts": {
    "tools": ["request_user_approval", "send_notification"]
  },
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "presenceTarget": {
        "type": "string",
        "description": "Agent ID to route presence_settled/presence_change events to. Default: 'main' (current behavior). Set to 'environmental' to route to the environmental agent.",
        "default": "main"
      }
    }
  }
}