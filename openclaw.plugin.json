{
  "id": "openclaw-interaction-bridge-v2",
  "name": "OpenClaw Interaction Bridge v2",
  "description": "Bridge OpenClaw agent state, approval requests, and notifications to Snarling (physical display + A/B buttons)",
  "categories": ["integrations"],
  "skills": ["skills"],
  "contracts": {
    "tools": ["request_user_approval", "send_notification"]
  },
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "presenceTarget": {
        "type": "string",
        "description": "Agent ID to route presence_settled/presence_change events to. Default: 'main' (current behavior). Set to 'environmental' to route to the environmental agent. Set to 'disabled' to disable event routing entirely.",
        "default": "main"
      },
      "environmentalEventsEnabled": {
        "type": "boolean",
        "description": "Whether to register the /environmental-event HTTP route and process incoming environmental/thermal events. Default: true. Set to false to completely disable environmental event processing (no route registered, no agent wake calls, no observation reports).",
        "default": true
      },
      "approvalSecret": {
        "type": "string",
        "description": "Secret for approval/notification callbacks from snarling. Optional — if unset, a random UUID is generated at gateway start (snarling echoes it from alert payloads).",
        "default": ""
      }
    }
  }
}